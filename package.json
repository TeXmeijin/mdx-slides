{
  "name": "slides",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "fusuma start -p 9999",
    "start:prod": "fusuma start-prod",
    "build": "fusuma build",
    "deploy": "npm run build && fusuma deploy",
    "pdf": "npm run build && fusuma pdf"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "fusuma": "^2.7.0"
  },
  "dependencies": {
    "@fusuma/task-pdf": "^2.7.0"
  }
}
